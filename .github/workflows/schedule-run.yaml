name: Meroshare run 
on:
    schedule:
      - cron: '0 9 * * *'
jobs:
    cypress-run:
        name: Meroshare run
        runs-on: ubuntu-22.04
        steps:
            - name: Checkout
            uses: actions/checkout@v4
            - name: Cypress run
            uses: cypress-io/github-action@v6
            with:
                record: false